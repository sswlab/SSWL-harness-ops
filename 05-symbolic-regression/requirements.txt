# 05-symbolic-regression — PyPI 패키지
# GitHub 기반(NeSymReS, AI Feynman)은 install.sh 참조

# --- Core scientific stack ---
numpy>=1.24
scipy>=1.11
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
sympy>=1.12

# --- SR: GP / 대표 ---
pysr>=0.19            # Julia 백엔드 자동 설치는 install.sh 참조
deap>=1.4

# --- SR: 딥러닝 / 동역학 ---
torch>=2.0            # pykan, NeSymReS, DSO 공통 의존
pykan>=0.2            # KAN
pysindy>=1.7          # 시계열 ODE/PDE 식별

# --- (선택) DSO ---
# pip 패키지명이 환경에 따라 다를 수 있어 install.sh에서 별도 처리
# dso

# --- 보조 ---
tqdm
pyyaml
joblib
